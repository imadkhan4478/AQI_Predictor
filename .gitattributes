# LF everywhere, so files authored on Windows and on the Linux runners stop
# churning against each other.
* text=auto eol=lf

# Never normalise binaries.
*.png binary
*.jpg binary
*.pdf binary
*.pkl binary
*.h5 binary
*.keras binary
