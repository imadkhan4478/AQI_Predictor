# The TensorFlow/Keras entry in the model comparison (training_pipeline/train.py).
#
# Separate from requirements.txt because pip cannot resolve the two together:
# hopsworks 5.0.3 requires protobuf<5, tensorflow 2.21 requires protobuf>=6.31.
# hopsworks' upper bound is over-tight in practice -- this project has run on
# protobuf 7.x throughout -- but pip refuses the combination regardless.
#
# Install second:
#     pip install -r requirements.txt
#     pip install -r requirements-deep.txt
#
# The second command upgrades protobuf and warns about hopsworks. That warning is
# expected. Nothing deployed needs this file -- the pipelines, API, dashboard and
# tests all run on requirements.txt alone.

tensorflow==2.21.0
