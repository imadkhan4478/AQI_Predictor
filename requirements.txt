# Pinned to the versions every measurement in docs/EXPERIMENT_LOG.md was taken
# against, so a GitHub runner installs the same stack as the local venv.

# Core
python-dotenv==1.2.2
requests==2.34.2
pandas==2.3.3
numpy==2.4.6

# Feature store / model registry
hopsworks==5.0.3
confluent-kafka==2.15.0

# Modeling
scikit-learn==1.9.0
joblib==1.5.3
statsmodels==0.14.6
# TensorFlow is in requirements-deep.txt: it cannot be resolved alongside
# hopsworks in one pip pass, and only the model comparison needs it.

# Explainability
shap==0.51.0

# Web app
streamlit==1.60.0
plotly==6.9.0
fastapi==0.140.0
uvicorn==0.51.0

# Testing
pytest==9.1.1
httpx==0.28.1   # required by fastapi.testclient

# EDA / plotting
matplotlib==3.11.1
seaborn==0.13.2
jupyter==1.1.1
nbconvert==7.17.1
ipykernel==7.3.0
