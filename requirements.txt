# Pinned to the versions this project was developed and evaluated against, so a
# GitHub runner installs the same stack as the local venv. Unpinned, a silent
# minor upgrade on the daily job can change model behaviour with no commit to
# point at -- and every measurement in docs/EXPERIMENT_LOG.md was taken here.

# Core
python-dotenv==1.2.2
requests==2.34.2
pandas==2.3.3
numpy==2.4.6

# Feature store / model registry
hopsworks==5.0.3
confluent-kafka==2.15.0

# Modeling
scikit-learn==1.9.0
joblib==1.5.3
statsmodels==0.14.6
# TensorFlow is NOT here -- see requirements-deep.txt. hopsworks 5.0.3 declares
# protobuf<5 while tensorflow 2.21 requires protobuf>=6.31, so pip cannot
# resolve both in one pass. Nothing in the deployed system needs TensorFlow:
# it is used only by the neural-net entry in the model comparison.

# Explainability
shap==0.51.0

# Web app
streamlit==1.60.0
plotly==6.9.0
fastapi==0.140.0
uvicorn==0.51.0

# Testing
pytest==9.1.1
httpx==0.28.1   # required by fastapi.testclient

# EDA / plotting
matplotlib==3.11.1
seaborn==0.13.2
jupyter==1.1.1
nbconvert==7.17.1
ipykernel==7.3.0
